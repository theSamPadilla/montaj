{
  "name": "rm_nonspeech",
  "description": "Remove all non-speech regions using whisper word-level timestamps",
  "input": {
    "type": "video_or_spec",
    "description": "Source video file or trim spec JSON from a prior step"
  },
  "output": {
    "type": "spec",
    "description": "Trim spec JSON containing only speech regions"
  },
  "params": [
    {
      "name": "model",
      "type": "string",
      "default": "base",
      "description": "Whisper model for speech detection. Larger = slower + more accurate."
    },
    {
      "name": "max-word-gap",
      "type": "float",
      "default": 0.18,
      "min": 0.0,
      "max": 1.0,
      "description": "Maximum gap between words to bridge in seconds. Larger = fewer cuts."
    },
    {
      "name": "sentence-edge",
      "type": "float",
      "default": 0.10,
      "min": 0.0,
      "max": 0.5,
      "description": "Padding to keep before and after each speech region in seconds"
    },
    {
      "name": "language",
      "type": "string",
      "default": "en",
      "description": "Whisper language code (e.g. es), or 'auto' to detect. A non-English value auto-upgrades an *.en model to its multilingual sibling — pass it for non-English audio, otherwise speech is mis-detected and cut as silence."
    }
  ]
}
