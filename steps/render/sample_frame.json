{
  "name": "sample_frame",
  "description": "Render a fully composited project frame at a given timestamp to PNG (video + image items + active overlay JSXs, no audio).",
  "input": {
    "type": "project",
    "description": "Absolute path to project.json"
  },
  "output": {
    "type": "image",
    "description": "PNG file at the requested output path (defaults to <project_dir>/render/samples/frame-<at>s.png)"
  },
  "params": [
    {
      "name": "project",
      "type": "path",
      "required": true,
      "description": "Absolute path to project.json"
    },
    {
      "name": "at",
      "type": "float",
      "required": true,
      "description": "Timestamp in seconds to sample"
    },
    {
      "name": "sdr-curve",
      "type": "string",
      "required": false,
      "description": "Look curve id for the HDR-to-SDR grade (see montaj_assets/luts/looks.json, e.g. 'vivid1'). Meaningless on SDR projects: ignored with a stderr warning, not an error. Omit to use the project's master look."
    },
    {
      "name": "out",
      "type": "path",
      "required": false,
      "description": "Output PNG path. Defaults to <project_dir>/render/samples/frame-<at>s.png"
    }
  ]
}
